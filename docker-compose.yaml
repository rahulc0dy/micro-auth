version: "3.8"

services:
  app:
    build:
      context: .
    ports:
      - "8000:8000"
    env_file:
      - .env
      - .env.production
    command: [ "bun", "run", "src/index.ts" ]
    healthcheck:
      test: [ "CMD", "curl", "-f", "http://localhost:8000/api/v1/health-check/server" ]
      interval: 30s
      timeout: 3s
      retries: 2